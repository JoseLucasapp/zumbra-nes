# Zumbra NES API

Generated by `zumbra doc`.

## `/home/joselucasapp/projects/zumbra-nes/src/core/apu.zum`

### Envelope

```zumbra
pub struct Envelope
```

Shared envelope generator used by pulse and noise channels.

| Member | Kind | Type / Signature |
|---|---|---|
| `start` | field | `bool` |
| `loop` | field | `bool` |
| `constantVolume` | field | `bool` |
| `volume` | field | `int` |
| `divider` | field | `int` |
| `decay` | field | `int` |
| `restart` | method | `fct restart(self)` |
| `clock` | method | `fct clock(self)` |
| `output` | method | `fct output(self)` |

### PulseChannel

```zumbra
pub struct PulseChannel
```

One of the two NES pulse channels.

| Member | Kind | Type / Signature |
|---|---|---|
| `enabled` | field | `bool` |
| `channelOne` | field | `bool` |
| `duty` | field | `int` |
| `sequence` | field | `int` |
| `timerPeriod` | field | `int` |
| `timerCounter` | field | `int` |
| `lengthCounter` | field | `int` |
| `envelope` | field | `Envelope` |
| `sweepEnabled` | field | `bool` |
| `sweepPeriod` | field | `int` |
| `sweepNegate` | field | `bool` |
| `sweepShift` | field | `int` |
| `sweepReload` | field | `bool` |
| `sweepDivider` | field | `int` |
| `writeControl` | method | `fct writeControl(self, value)` |
| `writeSweep` | method | `fct writeSweep(self, value)` |
| `writeTimerLow` | method | `fct writeTimerLow(self, value)` |
| `writeTimerHigh` | method | `fct writeTimerHigh(self, value)` |
| `sweepTarget` | method | `fct sweepTarget(self)` |
| `sweepMuted` | method | `fct sweepMuted(self)` |
| `tickTimer` | method | `fct tickTimer(self)` |
| `quarterFrame` | method | `fct quarterFrame(self)` |
| `halfFrame` | method | `fct halfFrame(self)` |
| `output` | method | `fct output(self)` |

### TriangleChannel

```zumbra
pub struct TriangleChannel
```

Triangle channel with linear and length counters.

| Member | Kind | Type / Signature |
|---|---|---|
| `enabled` | field | `bool` |
| `sequence` | field | `int` |
| `timerPeriod` | field | `int` |
| `timerCounter` | field | `int` |
| `lengthCounter` | field | `int` |
| `linearCounter` | field | `int` |
| `linearReload` | field | `int` |
| `controlFlag` | field | `bool` |
| `reloadFlag` | field | `bool` |
| `writeControl` | method | `fct writeControl(self, value)` |
| `writeTimerLow` | method | `fct writeTimerLow(self, value)` |
| `writeTimerHigh` | method | `fct writeTimerHigh(self, value)` |
| `tickTimer` | method | `fct tickTimer(self)` |
| `quarterFrame` | method | `fct quarterFrame(self)` |
| `halfFrame` | method | `fct halfFrame(self)` |
| `output` | method | `fct output(self)` |

### NoiseChannel

```zumbra
pub struct NoiseChannel
```

Noise channel with the hardware-style 15-bit LFSR.

| Member | Kind | Type / Signature |
|---|---|---|
| `enabled` | field | `bool` |
| `mode` | field | `bool` |
| `shiftRegister` | field | `int` |
| `timerPeriod` | field | `int` |
| `timerCounter` | field | `int` |
| `lengthCounter` | field | `int` |
| `envelope` | field | `Envelope` |
| `writeControl` | method | `fct writeControl(self, value)` |
| `writePeriod` | method | `fct writePeriod(self, value)` |
| `writeLength` | method | `fct writeLength(self, value)` |
| `tickTimer` | method | `fct tickTimer(self)` |
| `quarterFrame` | method | `fct quarterFrame(self)` |
| `halfFrame` | method | `fct halfFrame(self)` |
| `output` | method | `fct output(self)` |

### DmcChannel

```zumbra
pub struct DmcChannel
```

Delta modulation channel state. Memory fetches are supplied by the console bus.

| Member | Kind | Type / Signature |
|---|---|---|
| `enabled` | field | `bool` |
| `irqEnabled` | field | `bool` |
| `loop` | field | `bool` |
| `irq` | field | `bool` |
| `timerPeriod` | field | `int` |
| `timerCounter` | field | `int` |
| `outputLevel` | field | `int` |
| `sampleAddress` | field | `int` |
| `sampleLength` | field | `int` |
| `currentAddress` | field | `int` |
| `bytesRemaining` | field | `int` |
| `sampleBuffer` | field | `int` |
| `sampleBufferEmpty` | field | `bool` |
| `shiftRegister` | field | `int` |
| `bitsRemaining` | field | `int` |
| `silence` | field | `bool` |
| `restart` | method | `fct restart(self)` |
| `writeControl` | method | `fct writeControl(self, value)` |
| `tickTimer` | method | `fct tickTimer(self)` |

### ApuCore

```zumbra
pub struct ApuCore
```

Global APU frame sequencer, interrupt and sample-clock state.

| Member | Kind | Type / Signature |
|---|---|---|
| `cycle` | field | `u64` |
| `frameCycle` | field | `int` |
| `fiveStep` | field | `bool` |
| `irqInhibit` | field | `bool` |
| `frameIrq` | field | `bool` |
| `sampleDivider` | field | `int` |
| `sampleCount` | field | `u64` |
| `sampleWrite` | field | `int` |

### create

```zumbra
pub fct create()
```

Creates a reset APU package and deterministic PCM ring buffer.

### tick

```zumbra
pub fct tick(state)
```

Advances the APU by one CPU cycle.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### cpuWrite

```zumbra
pub fct cpuWrite(state, address, value)
```

Writes an APU register in the CPU-visible $4000-$4017 range.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### cpuReadStatus

```zumbra
pub fct cpuReadStatus(state)
```

Reads $4015 status and clears the frame IRQ latch.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### irqLine

```zumbra
pub fct irqLine(state)
```

Returns whether either APU interrupt source is asserted.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### dmcNeedsByte

```zumbra
pub fct dmcNeedsByte(state)
```

Returns whether the DMC sample buffer requires a CPU-bus byte.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### dmcAddress

```zumbra
pub fct dmcAddress(state)
```

Returns the current DMC CPU-bus address.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### supplyDmcByte

```zumbra
pub fct supplyDmcByte(state, value)
```

Supplies one DMC sample byte fetched by the console bus.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `value` | parameter | `` |

### sampleDigest

```zumbra
pub fct sampleDigest(state)
```

Stable digest of the deterministic PCM ring buffer.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### sampleCount

```zumbra
pub fct sampleCount(state)
```

Total number of generated PCM samples.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### sampleAt

```zumbra
pub fct sampleAt(state, index)
```

Exposes one deterministic sample for tests and headless frontends.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `index` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/bus.zum`

### create

```zumbra
pub fct create(cart)
```

Builds the complete Z21 CPU bus around one validated cartridge.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |

### read

```zumbra
pub fct read(state, address)
```

Reads one CPU-visible byte, including PPU, APU and controller side effects.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |

### write

```zumbra
pub fct write(state, address, value)
```

Writes one CPU-visible byte and routes all Z21 hardware registers.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### ppuRead

```zumbra
pub fct ppuRead(state, address)
```

Reads one PPU address through the attached PPU memory map.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |

### ppuWrite

```zumbra
pub fct ppuWrite(state, address, value)
```

Writes one PPU address through the attached PPU memory map.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### setController

```zumbra
pub fct setController(state, port, buttons)
```

Updates one controller's complete eight-button state.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `port` | parameter | `` |
| `buttons` | parameter | `` |

### performDma

```zumbra
pub fct performDma(state, cpuCycle)
```

Copies a full CPU page into PPU OAM and returns the 513/514-cycle CPU stall.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `cpuCycle` | parameter | `` |

### takePpuNmi

```zumbra
pub fct takePpuNmi(state)
```

Returns whether a sprite/background VBlank transition requested NMI.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### apuIrq

```zumbra
pub fct apuIrq(state)
```

Returns the combined APU IRQ line.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/cartridge.zum`

### Cartridge

```zumbra
pub struct Cartridge
```

Immutable metadata describing one loaded cartridge image.

| Member | Kind | Type / Signature |
|---|---|---|
| `header` | field | `header.RomHeader` |
| `sourcePath` | field | `string` |
| `digest` | field | `string` |
| `chrIsRam` | field | `bool` |
| `imageBytes` | field | `int` |
| `trainerBytes` | field | `int` |

### ImageInspection

```zumbra
pub struct ImageInspection
```

Non-fatal validation result for a complete ROM image.

| Member | Kind | Type / Signature |
|---|---|---|
| `ok` | field | `bool` |
| `message` | field | `string` |
| `expectedBytes` | field | `int` |
| `actualBytes` | field | `int` |

### inspectImage

```zumbra
pub fct inspectImage(data)
```

Validates header, trainer and ROM payload boundaries.

| Member | Kind | Type / Signature |
|---|---|---|
| `data` | parameter | `` |

### fromBytes

```zumbra
pub fct fromBytes(data, sourcePath)
```

Creates a cartridge package from an in-memory ROM image.  
The returned dictionary contains `info`, `prgRom`, `chrMemory` and `trainerData`.

| Member | Kind | Type / Signature |
|---|---|---|
| `data` | parameter | `` |
| `sourcePath` | parameter | `` |

### load

```zumbra
pub fct load(path)
```

Loads and validates a cartridge from disk.

| Member | Kind | Type / Signature |
|---|---|---|
| `path` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/clock.zum`

### Clock

```zumbra
pub struct Clock
```

Deterministic master clock shared by future CPU, PPU and APU implementations.

| Member | Kind | Type / Signature |
|---|---|---|
| `masterCycles` | field | `u64` |
| `cpuCycles` | field | `u64` |
| `ppuCycles` | field | `u64` |
| `frames` | field | `u64` |
| `running` | field | `bool` |
| `tick` | method | `fct tick(self)` |
| `stepMaster` | method | `fct stepMaster(self, count)` |
| `stepCpu` | method | `fct stepCpu(self, count)` |
| `stop` | method | `fct stop(self)` |
| `start` | method | `fct start(self)` |

### create

```zumbra
pub fct create()
```

Creates a clock at cycle zero.

## `/home/joselucasapp/projects/zumbra-nes/src/core/console.zum`

### ConsoleCore

```zumbra
pub struct ConsoleCore
```

Master scheduler and hardware accounting for one NES console.

| Member | Kind | Type / Signature |
|---|---|---|
| `cpuCycles` | field | `u64` |
| `ppuCycles` | field | `u64` |
| `apuCycles` | field | `u64` |
| `instructions` | field | `u64` |
| `dmaCycles` | field | `u64` |
| `dmcStallCycles` | field | `u64` |
| `frames` | field | `u64` |
| `running` | field | `bool` |

### create

```zumbra
pub fct create(cart)
```

Creates a complete Z21 console and clocks the seven reset cycles.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |

### stepInstruction

```zumbra
pub fct stepInstruction(machine)
```

Runs one CPU instruction and all associated PPU/APU/DMA/DMC cycles.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |

### runHardwareCycles

```zumbra
pub fct runHardwareCycles(machine, count)
```

Advances hardware clocks without executing CPU instructions.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |
| `count` | parameter | `` |

### runFrame

```zumbra
pub fct runFrame(machine, instructionLimit)
```

Runs instructions until the current PPU frame completes.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |
| `instructionLimit` | parameter | `` |

### setController

```zumbra
pub fct setController(machine, port, buttons)
```

Sets one controller's full button mask.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |
| `port` | parameter | `` |
| `buttons` | parameter | `` |

### frameDigest

```zumbra
pub fct frameDigest(machine)
```

Returns the current framebuffer digest.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |

### audioDigest

```zumbra
pub fct audioDigest(machine)
```

Returns the current audio-ring digest.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |

### stop

```zumbra
pub fct stop(machine)
```

Stops future instruction execution while leaving state inspectable.

| Member | Kind | Type / Signature |
|---|---|---|
| `machine` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/constants.zum`

### HEADER_BYTES

```zumbra
pub const HEADER_BYTES
```

Size of an iNES or NES 2.0 header.

### TRAINER_BYTES

```zumbra
pub const TRAINER_BYTES
```

Optional trainer size.

### PRG_BANK_BYTES

```zumbra
pub const PRG_BANK_BYTES
```

PRG ROM bank size.

### CHR_BANK_BYTES

```zumbra
pub const CHR_BANK_BYTES
```

CHR ROM bank size.

### CHR_RAM_BYTES

```zumbra
pub const CHR_RAM_BYTES
```

Default CHR RAM size for cartridges without CHR ROM.

### PRG_RAM_BYTES

```zumbra
pub const PRG_RAM_BYTES
```

NROM PRG RAM window size.

### CPU_RAM_BYTES

```zumbra
pub const CPU_RAM_BYTES
```

Internal CPU RAM size before mirroring.

### PPU_REGISTER_BYTES

```zumbra
pub const PPU_REGISTER_BYTES
```

Number of PPU registers before mirroring.

### APU_IO_BYTES

```zumbra
pub const APU_IO_BYTES
```

APU and I/O register window.

### PPU_WIDTH

```zumbra
pub const PPU_WIDTH
```

Visible NES framebuffer width.

### PPU_HEIGHT

```zumbra
pub const PPU_HEIGHT
```

Visible NES framebuffer height.

### PPU_DOTS_PER_SCANLINE

```zumbra
pub const PPU_DOTS_PER_SCANLINE
```

Dots per PPU scanline.

### PPU_SCANLINES_PER_FRAME

```zumbra
pub const PPU_SCANLINES_PER_FRAME
```

NTSC PPU scanlines including pre-render and VBlank.

### NTSC_PPU_CYCLES_PER_FRAME

```zumbra
pub const NTSC_PPU_CYCLES_PER_FRAME
```

PPU master cycles per normal NTSC frame.

### NAMETABLE_BYTES

```zumbra
pub const NAMETABLE_BYTES
```

Internal nametable RAM for horizontal/vertical mirroring.

### FOUR_SCREEN_NAMETABLE_BYTES

```zumbra
pub const FOUR_SCREEN_NAMETABLE_BYTES
```

Four-screen nametable RAM.

### PALETTE_BYTES

```zumbra
pub const PALETTE_BYTES
```

Palette RAM after mirroring.

### OAM_BYTES

```zumbra
pub const OAM_BYTES
```

Primary OAM capacity.

### FRAMEBUFFER_BYTES

```zumbra
pub const FRAMEBUFFER_BYTES
```

Palette-index framebuffer capacity.

### AUDIO_BUFFER_BYTES

```zumbra
pub const AUDIO_BUFFER_BYTES
```

Deterministic ring buffer used by the headless APU.

## `/home/joselucasapp/projects/zumbra-nes/src/core/controller.zum`

### BUTTON_A

```zumbra
pub const BUTTON_A
```

NES controller button bit for A.

### BUTTON_B

```zumbra
pub const BUTTON_B
```

NES controller button bit for B.

### BUTTON_SELECT

```zumbra
pub const BUTTON_SELECT
```

NES controller button bit for Select.

### BUTTON_START

```zumbra
pub const BUTTON_START
```

NES controller button bit for Start.

### BUTTON_UP

```zumbra
pub const BUTTON_UP
```

NES controller button bit for Up.

### BUTTON_DOWN

```zumbra
pub const BUTTON_DOWN
```

NES controller button bit for Down.

### BUTTON_LEFT

```zumbra
pub const BUTTON_LEFT
```

NES controller button bit for Left.

### BUTTON_RIGHT

```zumbra
pub const BUTTON_RIGHT
```

NES controller button bit for Right.

### Controller

```zumbra
pub struct Controller
```

Serial controller state exposed through CPU addresses $4016 and $4017.

| Member | Kind | Type / Signature |
|---|---|---|
| `buttons` | field | `int` |
| `latched` | field | `int` |
| `index` | field | `int` |
| `strobe` | field | `bool` |
| `setButtons` | method | `fct setButtons(self, value)` |
| `writeStrobe` | method | `fct writeStrobe(self, value)` |
| `read` | method | `fct read(self)` |

### create

```zumbra
pub fct create()
```

Creates a released controller with no buttons pressed.

## `/home/joselucasapp/projects/zumbra-nes/src/core/cpu6502.zum`

### FLAG_CARRY

```zumbra
pub const FLAG_CARRY
```

Carry flag.

### FLAG_ZERO

```zumbra
pub const FLAG_ZERO
```

Zero flag.

### FLAG_INTERRUPT

```zumbra
pub const FLAG_INTERRUPT
```

Interrupt-disable flag.

### FLAG_DECIMAL

```zumbra
pub const FLAG_DECIMAL
```

Decimal flag. The NES 2A03 stores it but ADC/SBC remain binary.

### FLAG_BREAK

```zumbra
pub const FLAG_BREAK
```

Break marker used only in values pushed to the stack.

### FLAG_UNUSED

```zumbra
pub const FLAG_UNUSED
```

Unused status bit, kept set by the CPU core.

### FLAG_OVERFLOW

```zumbra
pub const FLAG_OVERFLOW
```

Overflow flag.

### FLAG_NEGATIVE

```zumbra
pub const FLAG_NEGATIVE
```

Negative flag.

### VECTOR_NMI

```zumbra
pub const VECTOR_NMI
```

### VECTOR_RESET

```zumbra
pub const VECTOR_RESET
```

### VECTOR_IRQ

```zumbra
pub const VECTOR_IRQ
```

### STACK_BASE

```zumbra
pub const STACK_BASE
```

### OFFICIAL_OPCODE_COUNT

```zumbra
pub const OFFICIAL_OPCODE_COUNT
```

### AddressResult

```zumbra
pub struct AddressResult
```

Address calculation result used by indexed modes.

| Member | Kind | Type / Signature |
|---|---|---|
| `address` | field | `int` |
| `pageCrossed` | field | `bool` |

### OpcodeInfo

```zumbra
pub struct OpcodeInfo
```

Stable metadata for one opcode in the official NMOS 6502 table.

| Member | Kind | Type / Signature |
|---|---|---|
| `opcode` | field | `int` |
| `mnemonic` | field | `string` |
| `mode` | field | `string` |
| `bytes` | field | `int` |
| `cycles` | field | `int` |
| `pageCrossPenalty` | field | `bool` |
| `official` | field | `bool` |

### opcodeInfo

```zumbra
pub fct opcodeInfo(opcode)
```

Returns metadata for an official opcode or an explicit illegal marker.

| Member | Kind | Type / Signature |
|---|---|---|
| `opcode` | parameter | `` |

### isOfficialOpcode

```zumbra
pub fct isOfficialOpcode(opcode)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `opcode` | parameter | `` |

### officialOpcodeCount

```zumbra
pub fct officialOpcodeCount()
```

### Cpu6502

```zumbra
pub struct Cpu6502
```

Complete Ricoh 2A03 CPU state. Decimal arithmetic is intentionally disabled.

| Member | Kind | Type / Signature |
|---|---|---|
| `a` | field | `int` |
| `x` | field | `int` |
| `y` | field | `int` |
| `sp` | field | `int` |
| `pc` | field | `int` |
| `status` | field | `int` |
| `cycles` | field | `int` |
| `instructions` | field | `int` |
| `lastOpcode` | field | `int` |
| `halted` | field | `bool` |
| `irqLine` | field | `bool` |
| `nmiPending` | field | `bool` |
| `getFlag` | method | `fct getFlag(self, mask)` |
| `setFlag` | method | `fct setFlag(self, mask, enabled)` |
| `setZeroNegative` | method | `fct setZeroNegative(self, value)` |
| `read` | method | `fct read(self, systemBus, address)` |
| `write` | method | `fct write(self, systemBus, address, value)` |
| `fetchByte` | method | `fct fetchByte(self, systemBus)` |
| `fetchWord` | method | `fct fetchWord(self, systemBus)` |
| `readWord` | method | `fct readWord(self, systemBus, address)` |
| `readWordBug` | method | `fct readWordBug(self, systemBus, address)` |
| `pushByte` | method | `fct pushByte(self, systemBus, value)` |
| `popByte` | method | `fct popByte(self, systemBus)` |
| `pushWord` | method | `fct pushWord(self, systemBus, value)` |
| `popWord` | method | `fct popWord(self, systemBus)` |
| `addressZeroPage` | method | `fct addressZeroPage(self, systemBus)` |
| `addressZeroPageX` | method | `fct addressZeroPageX(self, systemBus)` |
| `addressZeroPageY` | method | `fct addressZeroPageY(self, systemBus)` |
| `addressAbsolute` | method | `fct addressAbsolute(self, systemBus)` |
| `addressAbsoluteX` | method | `fct addressAbsoluteX(self, systemBus)` |
| `addressAbsoluteY` | method | `fct addressAbsoluteY(self, systemBus)` |
| `addressIndexedIndirect` | method | `fct addressIndexedIndirect(self, systemBus)` |
| `addressIndirectIndexed` | method | `fct addressIndirectIndexed(self, systemBus)` |
| `branch` | method | `fct branch(self, systemBus, condition)` |
| `adc` | method | `fct adc(self, value)` |
| `sbc` | method | `fct sbc(self, value)` |
| `compare` | method | `fct compare(self, registerValue, value)` |
| `bit` | method | `fct bit(self, value)` |
| `asl` | method | `fct asl(self, value)` |
| `lsr` | method | `fct lsr(self, value)` |
| `rol` | method | `fct rol(self, value)` |
| `ror` | method | `fct ror(self, value)` |
| `serviceInterrupt` | method | `fct serviceInterrupt(self, systemBus, vector)` |
| `reset` | method | `fct reset(self, systemBus)` |
| `requestNmi` | method | `fct requestNmi(self)` |
| `requestIrq` | method | `fct requestIrq(self)` |
| `clearIrq` | method | `fct clearIrq(self)` |
| `step` | method | `fct step(self, systemBus)` |

### create

```zumbra
pub fct create(systemBus)
```

Creates and resets a CPU against the supplied Z19 bus.

| Member | Kind | Type / Signature |
|---|---|---|
| `systemBus` | parameter | `` |

### run

```zumbra
pub fct run(processor, systemBus, instructionLimit)
```

Executes at most `instructionLimit` instructions and returns the number run.

| Member | Kind | Type / Signature |
|---|---|---|
| `processor` | parameter | `` |
| `systemBus` | parameter | `` |
| `instructionLimit` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/devices.zum`

### CpuPort

```zumbra
pub struct CpuPort
```

External interrupt and lifecycle contract shared with the CPU core.

| Member | Kind | Type / Signature |
|---|---|---|
| `resetPending` | field | `bool` |
| `nmiPending` | field | `bool` |
| `irqPending` | field | `bool` |
| `halted` | field | `bool` |

### PpuPort

```zumbra
pub struct PpuPort
```

Observable Ricoh 2C02 timing state.

| Member | Kind | Type / Signature |
|---|---|---|
| `scanline` | field | `int` |
| `dot` | field | `int` |
| `frame` | field | `u64` |
| `frameComplete` | field | `bool` |
| `nmiPending` | field | `bool` |

### ApuPort

```zumbra
pub struct ApuPort
```

Observable APU timing, IRQ and sample state.

| Member | Kind | Type / Signature |
|---|---|---|
| `sampleClock` | field | `u64` |
| `frameCounter` | field | `int` |
| `frameIrq` | field | `bool` |
| `dmcIrq` | field | `bool` |

### ControllerPort

```zumbra
pub struct ControllerPort
```

Observable serial controller state.

| Member | Kind | Type / Signature |
|---|---|---|
| `buttons` | field | `int` |
| `latched` | field | `int` |
| `index` | field | `int` |
| `strobe` | field | `bool` |

### DmaPort

```zumbra
pub struct DmaPort
```

Observable OAM DMA transaction state.

| Member | Kind | Type / Signature |
|---|---|---|
| `pending` | field | `bool` |
| `page` | field | `int` |
| `cycles` | field | `int` |

### createCpuPort

```zumbra
pub fct createCpuPort()
```

### createPpuPort

```zumbra
pub fct createPpuPort()
```

### createApuPort

```zumbra
pub fct createApuPort()
```

### createControllerPort

```zumbra
pub fct createControllerPort()
```

### createDmaPort

```zumbra
pub fct createDmaPort()
```

## `/home/joselucasapp/projects/zumbra-nes/src/core/header.zum`

### RomHeader

```zumbra
pub struct RomHeader
```

Parsed iNES/NES 2.0 cartridge header.

| Member | Kind | Type / Signature |
|---|---|---|
| `format` | field | `string` |
| `mapper` | field | `u16` |
| `submapper` | field | `u8` |
| `prgRomBytes` | field | `int` |
| `chrRomBytes` | field | `int` |
| `prgRamBytes` | field | `int` |
| `chrRamBytes` | field | `int` |
| `mirroring` | field | `string` |
| `battery` | field | `bool` |
| `trainer` | field | `bool` |
| `fourScreen` | field | `bool` |
| `consoleType` | field | `u8` |
| `timingMode` | field | `u8` |
| `rawFlags6` | field | `u8` |
| `rawFlags7` | field | `u8` |

### HeaderInspection

```zumbra
pub struct HeaderInspection
```

Result used to inspect untrusted headers without aborting execution.

| Member | Kind | Type / Signature |
|---|---|---|
| `ok` | field | `bool` |
| `message` | field | `string` |
| `header` | field | `RomHeader` |

### inspect

```zumbra
pub fct inspect(data)
```

Inspects a binary buffer and returns a structured validation result.

| Member | Kind | Type / Signature |
|---|---|---|
| `data` | parameter | `` |

### parse

```zumbra
pub fct parse(data)
```

Parses a trusted header and aborts with a clear message when invalid.

| Member | Kind | Type / Signature |
|---|---|---|
| `data` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/mapper.zum`

### Mapping

```zumbra
pub struct Mapping
```

Result of translating a CPU or PPU address through a mapper.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapped` | field | `bool` |
| `offset` | field | `int` |
| `writable` | field | `bool` |

### MapperDescriptor

```zumbra
pub struct MapperDescriptor
```

Runtime mapper contract. Z19 supports only Mapper 0/NROM.

| Member | Kind | Type / Signature |
|---|---|---|
| `id` | field | `u16` |
| `name` | field | `string` |
| `prgBanks` | field | `int` |
| `chrBanks` | field | `int` |

### supports

```zumbra
pub fct supports(mapperId)
```

Returns whether the mapper is implemented in the current emulator version.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |

### create

```zumbra
pub fct create(mapperId, prgBytes, chrBytes)
```

Creates the mapper descriptor or aborts for unsupported cartridges.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |
| `prgBytes` | parameter | `` |
| `chrBytes` | parameter | `` |

### cpuRead

```zumbra
pub fct cpuRead(mapperId, address, prgBytes)
```

Maps CPU reads in the PRG ROM window. Addresses are represented as int in Z19.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |
| `address` | parameter | `` |
| `prgBytes` | parameter | `` |

### cpuWrite

```zumbra
pub fct cpuWrite(mapperId, address, prgBytes)
```

NROM PRG is read-only.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |
| `address` | parameter | `` |
| `prgBytes` | parameter | `` |

### ppuRead

```zumbra
pub fct ppuRead(mapperId, address, chrBytes)
```

Maps PPU pattern-table reads and CHR RAM writes.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |
| `address` | parameter | `` |
| `chrBytes` | parameter | `` |

### ppuWrite

```zumbra
pub fct ppuWrite(mapperId, address, chrBytes, chrIsRam)
```

CHR is writable only when the cartridge uses CHR RAM.

| Member | Kind | Type / Signature |
|---|---|---|
| `mapperId` | parameter | `` |
| `address` | parameter | `` |
| `chrBytes` | parameter | `` |
| `chrIsRam` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/core/ppu.zum`

### Pixel

```zumbra
pub struct Pixel
```

One decoded pixel before final background/sprite priority selection.

| Member | Kind | Type / Signature |
|---|---|---|
| `color` | field | `int` |
| `opaque` | field | `bool` |
| `behindBackground` | field | `bool` |
| `spriteZero` | field | `bool` |

### PpuCore

```zumbra
pub struct PpuCore
```

CPU-visible and timing state for the Ricoh 2C02 PPU.

| Member | Kind | Type / Signature |
|---|---|---|
| `control` | field | `int` |
| `mask` | field | `int` |
| `status` | field | `int` |
| `oamAddress` | field | `int` |
| `v` | field | `int` |
| `t` | field | `int` |
| `fineX` | field | `int` |
| `writeToggle` | field | `bool` |
| `readBuffer` | field | `int` |
| `openBus` | field | `int` |
| `scrollX` | field | `int` |
| `scrollY` | field | `int` |
| `scanline` | field | `int` |
| `dot` | field | `int` |
| `cycles` | field | `u64` |
| `frame` | field | `u64` |
| `frameComplete` | field | `bool` |
| `nmiLine` | field | `bool` |
| `nmiPending` | field | `bool` |
| `oddFrame` | field | `bool` |
| `spritesOnLine` | field | `int` |

### create

```zumbra
pub fct create(cart)
```

Creates a power-on PPU package with VRAM, palette, OAM and framebuffer.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |

### read

```zumbra
pub fct read(state, address)
```

Reads the complete PPU address space after mirroring.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |

### write

```zumbra
pub fct write(state, address, value)
```

Writes CHR RAM, nametable RAM or palette RAM after mirroring.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### cpuRead

```zumbra
pub fct cpuRead(state, register)
```

Reads one of the eight CPU-visible PPU registers.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `register` | parameter | `` |

### cpuWrite

```zumbra
pub fct cpuWrite(state, register, value)
```

Writes one of the eight CPU-visible PPU registers.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `register` | parameter | `` |
| `value` | parameter | `` |

### tick

```zumbra
pub fct tick(state)
```

Advances one PPU dot and returns true when a new frame completes.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### takeNmi

```zumbra
pub fct takeNmi(state)
```

Returns and clears the edge-triggered PPU NMI request.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### oamDmaWrite

```zumbra
pub fct oamDmaWrite(state, value)
```

Writes one OAM byte and increments OAMADDR, as used by $4014 DMA.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |
| `value` | parameter | `` |

### clearFrameComplete

```zumbra
pub fct clearFrameComplete(state)
```

Clears the frame-complete edge after the frontend consumes it.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

### frameDigest

```zumbra
pub fct frameDigest(state)
```

Returns a stable digest of the 256x240 palette-index framebuffer.

| Member | Kind | Type / Signature |
|---|---|---|
| `state` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/frontend/desktop_contract.zum`

### DesktopFrontendState

```zumbra
pub struct DesktopFrontendState
```

UI-neutral state that a desktop frontend will render in Z21/Z22.

| Member | Kind | Type / Signature |
|---|---|---|
| `title` | field | `string` |
| `romLoaded` | field | `bool` |
| `paused` | field | `bool` |
| `status` | field | `string` |

### createState

```zumbra
pub fct createState()
```

Creates the initial desktop frontend contract without initializing SDL.

## `/home/joselucasapp/projects/zumbra-nes/src/frontend/headless.zum`

### DiagnosticReport

```zumbra
pub struct DiagnosticReport
```

Stable Z21 report covering CPU, PPU, APU, controllers and scheduler state.

| Member | Kind | Type / Signature |
|---|---|---|
| `sourcePath` | field | `string` |
| `format` | field | `string` |
| `mapper` | field | `u16` |
| `resetVector` | field | `u16` |
| `cpuA` | field | `int` |
| `cpuInstructions` | field | `int` |
| `cpuCycles` | field | `u64` |
| `ppuCycles` | field | `u64` |
| `ppuFrame` | field | `u64` |
| `ppuScanline` | field | `int` |
| `ppuDot` | field | `int` |
| `frameDigest` | field | `string` |
| `audioSamples` | field | `u64` |
| `audioDigest` | field | `string` |
| `controllerA` | field | `int` |
| `dmaCycles` | field | `u64` |
| `dmcStallCycles` | field | `u64` |

### run

```zumbra
pub fct run(cart)
```

Runs a deterministic full-hardware diagnostic without opening a window.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |

### printReport

```zumbra
pub fct printReport(report)
```

Prints the Z21 report in a deterministic VM/native format.

| Member | Kind | Type / Signature |
|---|---|---|
| `report` | parameter | `` |

### runFile

```zumbra
pub fct runFile(path)
```

Loads a ROM and runs the deterministic Z21 frontend.

| Member | Kind | Type / Signature |
|---|---|---|
| `path` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/persistence/metadata.zum`

### toJson

```zumbra
pub fct toJson(cart)
```

Converts cartridge metadata to stable JSON without serializing ROM bytes.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |

### save

```zumbra
pub fct save(path, cart)
```

Writes metadata only; ROM contents are never persisted by this function.

| Member | Kind | Type / Signature |
|---|---|---|
| `path` | parameter | `` |
| `cart` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/testing/assert.zum`

### assertBool

```zumbra
pub fct assertBool(condition, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `condition` | parameter | `` |
| `label` | parameter | `` |

### assertInt

```zumbra
pub fct assertInt(actual, expected, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `actual` | parameter | `` |
| `expected` | parameter | `` |
| `label` | parameter | `` |

### assertU8

```zumbra
pub fct assertU8(actual, expected, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `actual` | parameter | `` |
| `expected` | parameter | `` |
| `label` | parameter | `` |

### assertU16

```zumbra
pub fct assertU16(actual, expected, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `actual` | parameter | `` |
| `expected` | parameter | `` |
| `label` | parameter | `` |

### assertU64

```zumbra
pub fct assertU64(actual, expected, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `actual` | parameter | `` |
| `expected` | parameter | `` |
| `label` | parameter | `` |

### assertString

```zumbra
pub fct assertString(actual, expected, label)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `actual` | parameter | `` |
| `expected` | parameter | `` |
| `label` | parameter | `` |

## `/home/joselucasapp/projects/zumbra-nes/src/testing/cpu_fixture.zum`

### prgOffset

```zumbra
pub fct prgOffset(cart, address)
```

Returns the physical PRG offset for an NROM CPU address.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |
| `address` | parameter | `` |

### setByte

```zumbra
pub fct setByte(cart, address, value)
```

Mutates the synthetic in-memory PRG image used only by tests and diagnostics.

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### setWord

```zumbra
pub fct setWord(cart, address, value)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### installProgram

```zumbra
pub fct installProgram(cart, start, program)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `cart` | parameter | `` |
| `start` | parameter | `` |
| `program` | parameter | `` |

### createMachine

```zumbra
pub fct createMachine(start, program)
```

Creates a fresh Mapper 0 machine with reset/NMI/IRQ vectors initialized.

| Member | Kind | Type / Signature |
|---|---|---|
| `start` | parameter | `` |
| `program` | parameter | `` |

### writeByte

```zumbra
pub fct writeByte(systemBus, address, value)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `systemBus` | parameter | `` |
| `address` | parameter | `` |
| `value` | parameter | `` |

### readByte

```zumbra
pub fct readByte(systemBus, address)
```

| Member | Kind | Type / Signature |
|---|---|---|
| `systemBus` | parameter | `` |
| `address` | parameter | `` |

