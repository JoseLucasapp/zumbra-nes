# zumbra-nes 0.5.20 — safe no-ROM shell, icon intro and hard Mapper 227 guard

- Replaces the startup UI panel path with a safe notification/status path.
- Uses the packaged icon bitmap as the intro image when available.
- Keeps Mapper 227 behind a conservative guard to avoid desktop freezes.
- Fixes version gates for 0.5.20.
